# Paths omitted when publishing this repo with `clawhub package publish` (GitHub or local root).
# release/ — same tree as `npm run release` output; not tracked in git (see .gitignore).
release/
scripts/
DEVELOPMENT.md
.cursor/
discord-tts-attacher.code-workspace
.gitignore
.clawhubignore
node_modules